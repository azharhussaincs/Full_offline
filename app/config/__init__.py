"""Configuration & logging for the application.

Importing :data:`app.config.settings.settings` gives you the fully-resolved,
immutable application configuration (loaded from ``.env`` / environment).
"""
